class Calculator extends React.Component {
  render() {
    const id = fetch(`aoeunsth${id}`);
    return <h1>Hi there</h1>;
  }
}

const response = axios.get("url");

ReactDOM.render(<Calculator />, document.querySelector("#app"));
